# Release v1.0.10

## Fixes

- **Fix bấm ngoài để đóng SelectField sheet** — Bấm vào vùng tối bên ngoài bottom sheet (ví chọn, loại giao dịch, danh mục...) giờ luôn đóng đúng, kể cả khi bấm vào mép, handle bar hay vùng rìa
- **Fix vuốt Back đóng sheet thay vì về tổng quan** — Android Back button giờ chỉ đóng sheet đang mở, không navigate ra ngoài
- **Fix SelectField nháy khi bấm** — Loại bỏ double-fire giữa native select và visual trigger
- **Fix danh mục nháy/phải giữ mới chọn được** — Thêm `touch-action: manipulation` và `-webkit-tap-highlight-color: transparent`
- **Label filter 1 dòng** — Truncate ellipsis, không xuống dòng
- **Gọn label "Tất cả"** — Thay "Tất cả loại / ví / danh mục" thành "Tất cả"
